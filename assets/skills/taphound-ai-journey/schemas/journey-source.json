{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "TapHound Journey Source v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "kind", "name", "includes", "steps"],
  "properties": {
    "version": { "const": 1 },
    "kind": { "const": "journeySource" },
    "name": { "type": "string", "minLength": 1 },
    "includes": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*(/[A-Za-z0-9][A-Za-z0-9._-]*)*$"
      }
    },
    "steps": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "object" }
    }
  }
}
